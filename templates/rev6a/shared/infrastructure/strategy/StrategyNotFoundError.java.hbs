package {{rootPackage}}.shared.infrastructure.strategy;

public class StrategyNotFoundError extends RuntimeException {

  public StrategyNotFoundError(Class<?> type, String name) {
    super("No strategy found for type=" + type.getName() + " name=" + name);
  }
}

