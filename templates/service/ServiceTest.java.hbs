package {{basePackage}};

import io.quarkus.test.junit.QuarkusTest;
import org.junit.jupiter.api.Test;

import static io.restassured.RestAssured.given;
import static org.hamcrest.Matchers.is;

@QuarkusTest
class ServiceTest {

  @Test
  void healthz_should_return_ok() {
    given()
      .when().get("/healthz")
      .then()
      .statusCode(200)
      .body("status", is("ok"))
      .body("service", is("{{serviceName}}"));
  }
}

