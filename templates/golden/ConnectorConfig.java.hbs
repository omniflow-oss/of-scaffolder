package {{basePackage}}.config;

import io.smallrye.config.ConfigMapping;

@ConfigMapping(prefix = "connectors.{{kebab connectorName}}")
public interface {{pascal connectorName}}ConnectorConfig {
  String baseUrl();
}

